const { SIGN_IN, SIGN_UP, SIGN_OUT, SELECT_LANG, FAVORITE } = require('../../paths')

module.exports = {
    signInPath: SIGN_IN,
    signUpPath: SIGN_UP,
    signOutPath: SIGN_OUT,
    selectLangPath: SELECT_LANG,
    favoritePath: FAVORITE
}