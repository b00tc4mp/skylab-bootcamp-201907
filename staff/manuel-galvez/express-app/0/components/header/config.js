const { SIGN_IN, SIGN_UP, SIGN_OUT, FAVORITES } = require('../../constants')

module.exports = {
    signInPath: SIGN_IN,
    signUpPath: SIGN_UP,
    signOutPath: SIGN_OUT,
    favoritesPath: FAVORITES
}
