/**
 * Checks wether a testing result matches an expected value. In case of matching values, 
 * it does nothing. Otherwise shows an error explaining the unmatching situation.
 * 
 * @param {*} result  The testing value to evaluate.
 * @param {*} result The expected value to check the result against.
 */

/*function check0(result, expected) {
    if (result.toString() !== expected.toString()) { 
        console.error('error: result does not match expected value') 
    } else if (typeof result === "undefined" &&  typeof result !== typeof expected) {
        console.error('error: result does not match expected value') 
    }
}*/

function check() {
    
}