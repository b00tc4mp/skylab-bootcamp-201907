import React from 'react'

export default function(/* add methods */) {
    return <>
    <main class="main"> 
        <section class="register-success">
            <p>User successfully registered! You can now proceed to <a class="register-success__link" href={`/#/sign-in`} >Sign in</a></p>
        </section>
    </main>
    </>
}