{
  const { random } = Math

  describe('logic - retrieve article', () => {
    let user

    beforeEach(() =>
      user = {
        name: 'John-' + random(),
        surname: 'Doe-' + random(),
        username: 'johndoe-' +random() + '@mail.com',
        password: '123-' + random(),
        favorites: []
      })

      /* it('should succees on valid article url', () => {
        const url = // artículo
      }) */
  })
}